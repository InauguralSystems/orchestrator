# FINDINGS — the test suite's gap ledger

Every CLI bug this suite surfaces gets recorded here and routed to
`orchestrator-cli-engineer`. An empty ledger after a green run is the healthy
shape.

## Open

_None._

## Resolved

- 2026-07-07 — suite seeded (init, config, health, sync, hook, agentic, CLI
  surface + the bash -n bar). All green on first full run.
